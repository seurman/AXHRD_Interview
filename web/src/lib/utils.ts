/** 레거시 `@/lib/utils` — 라벨 헬퍼 + cn 재수출 */
export {
  competencyLabel,
  jobRoleLabel,
  companySizeLabel,
  formatPercentile,
  thetaToLevel,
  dimensionLabel,
} from "./labels";

export { cn } from "@/lib/cn";
